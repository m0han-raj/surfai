# Dependencies for the Vercel deployment.
#
# Deliberately a subset of backend/requirements.txt, at identical pins:
#
#   uvicorn  Vercel invokes the ASGI app directly, so no server is needed.
#   alembic  Migrations are run as a separate, deliberate step, never at
#            request time on a serverless host.
#
# Both are several megabytes and every one counts against the lambda size
# limit. tests/test_packaging.py fails if a shared pin drifts from the
# backend list.
fastapi==0.115.6
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
httpx==0.28.1
python-dotenv==1.0.1
