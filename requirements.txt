# Dependencies for the Vercel deployment.
#
# Vercel's parser does not support `-r` includes, so this list is inlined
# rather than pointing at backend/requirements.txt. The two are kept
# identical by tests/test_packaging.py, which fails if they drift.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
httpx==0.28.1
python-dotenv==1.0.1
